Review staged + unstaged changes against CLAUDE.md conventions.

1. Run `git diff` and `git diff --cached`
2. Check against all rules in CLAUDE.md
3. Output:

### Verdict: APPROVE | REQUEST_CHANGES
**Required Fixes**: [file:line] description
**Warnings**: [file:line] description

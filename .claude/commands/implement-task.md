Implement following CLAUDE.md conventions. Check existing `Features/` for reference patterns.

Task: $ARGUMENTS

Order: Domain -> Application -> Infrastructure -> Api
Output: changed files, summary, assumptions, gaps.
Rules: match existing style, minimal changes, no unrelated refactoring.
